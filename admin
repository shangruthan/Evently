# In a separate terminal:
docker-compose exec db psql -U user -d evently -c "UPDATE users SET role = 'admin' WHERE email = 'your-admin-email@example.com';"

migrate -path ./migrations -database "postgres://user:password@localhost:5432/evently?sslmode=disable" up